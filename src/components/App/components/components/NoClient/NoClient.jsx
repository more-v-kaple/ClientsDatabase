import React from 'react';

import './noClient.scss';

const NoClient = () => (
    <div className="no-selected-client">
        <p>
            Select client from the list on the left
            in order to see a detailed information
        </p>
    </div>
);

export default NoClient;
